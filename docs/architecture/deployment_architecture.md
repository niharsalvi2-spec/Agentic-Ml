# Deployment Architecture

Models are packaged into standalone `model.pkl` inference pipelines containing complete preprocessing transforms, scalers, and fitted estimators alongside `model_metadata.json`.
